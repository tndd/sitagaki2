# 2025/04/29
featureというクラスを作ったはいいが、strategyにおいてはこれと予測値のペアがなければシミュレーションを行うことができない。
だが今のところfeatureと予測値には特別な関連性を定義してはいない。

この２つをくっつける作業というのは定型作業になるだろうから、この合体後のクラスについても何らかの定義はあった方がいい気もするが、組み合わせただけで新クラスというのも違和感あり。

**featureに予測値を渡して表示させる関数を実装？**
ありかな。これならfeatureが予測値という状態を持つなんて厄介な状況にならずに済む。

# 2025/04/20
**strategy構想**
コンセプトとしてはモデルを評価することにある。
さらにいえば、モデルの予測値を元にした取引戦略を評価する。
モデルの評価だけならば、それはmodel内で行われるべきところだ。

だったらこのstrategyが受け取る引数とはどうなるか？

**strategyの引数**
model
これは複数受け取り得るな。
プロトタイプでは、簡略化のため単一のモデルを受け取ることになるだろう。

df
モデルが評価するためのデータは確実に必要だ。
今のところ、dfとモデルの関連性を明示する構造がない。
dfから見たら、どのモデルから処理されるかは知ったことではない。
むしろモデルの方がどのdfを対象にしているかという依存を持ってる。

**strategyの出力**
まず何の項目を出力とするかを決めないといけない。
さらにその形式や区分けも。

**dfとmodelの関係性はどこの責務？**
modelの段階では関数の型が指定されてるから、dfとmodelの渡し間違いが起こる余地はなかった。
だがstrategyレイヤーにおいては？
